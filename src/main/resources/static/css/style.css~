form {
    width: 90vw;
    max-width: 20em;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
}

span {
    font-size: larger;
    font-weight: bold;
    position: absolute;
}

button {
    margin-top: 2rem;
    background-color: blue;
    color: white;
    border: 1px solid;
    padding: 0.3em;
    width: 60%;
}

input {
    border: 1px solid;
    padding: 0.3em;
    width: 100%;
}

textarea {
    width: 100%;
    min-height: 6em;
}

label {
    display: block;
    font-size: 1.2rem;
    margin-top: 1rem;
}

select {
    font-size: 16px;
}

.postal-code {
    width: 50%;
}